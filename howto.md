# ⚡ Quick Restart & Demo Guide

If you need to instantly compile, launch, and physically demonstrate the exact scope of the **GigAegis Zomato Integration** to hackathon judges natively without wasting minutes, follow completely these strict sequences.

## 1. Boot up the Internal Database Constraints
Ensure you have MongoDB running physically on your machine natively securing atomic logic operations.
```bash
brew services start mongodb-community
```
*(If you are on Windows/Linux, start `mongod` natively instead of using brew).*

## 2. Boot up the Core Actuarial Backend
Start the Node.js Express platform routing server. This spins up the dynamic `Hyper-Local Pricing Engine` and the `Triple-Threat Oracular Hooks`.
```bash
cd backend
npm install
node index.js
```
*(The terminal logs will boldly display `[GigAegis Backend] Active on Port 3000`)*

## 3. Boot up the Zomato-Tuned React Visualizer
Open a new terminal specifically targeting the frontend react array explicitly.
```bash
cd gigaegis-frontend
npm install
npm run dev
```

## 4. The Pristine 45-Second Pitch Execution Flow
To cleanly articulate the architecture to the judging panels properly natively:

1. **The Registration Map:** Open `http://localhost:5173`. Look directly at the `Sign Up` organically formatted red bounds explicitly mapping Zomato variables. Type any worker name and type an arbitrary **Zomato Rider ID** cleanly. Select `Koramangala (High Vuln)` grids completely ensuring the +1.2x Pricing limit applies securely! Click Activate.
2. **The Worker UI Array:** The system automatically shifts into the mobile Worker HUD mapping their newly generated *Weekly Premium* and precisely evaluating their physical Risk limit fractions explicitly natively out of `1.0`.
3. **Treasury Augmentation:** Switch back specifically into the `Command Center` using the nav. Hit `COLLECT WEEKLY PREMIUMS` cleanly to dynamically boost the global Reserve Pool explicitly validating database writes intuitively visually.
4. **Demonstrating Payout Detonations:** In the `Parametric Simulator`, drag the `FLOOD` string array manually above `50mm` securely and click `EXECUTE PAYLOAD`.
5. **The Explainable Output Hooks:** Watch the Top Banner literally display state nodes natively (`[DETECTING]` -> `[FRAUD ANALYSIS]` -> `[PAYOUT]`) allowing non-technical judging scopes to exactly visualize the calculations cleanly!
6. **Last Event Summary:** Point out to the panels strictly how the Event Block parsed the integers correctly rendering *🚨 Flood → 💰 X paid → 🛡️ Y blocked → ₹Z total* proving your **Multi-Signal Fraud Integrity** physically dropped spoofed devices entirely natively.
